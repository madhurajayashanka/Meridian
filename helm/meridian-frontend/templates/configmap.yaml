apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "meridian-frontend.fullname" . }}-config
  labels:
    {{- include "meridian-frontend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
