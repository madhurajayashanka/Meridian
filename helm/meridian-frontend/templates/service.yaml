apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian-frontend.fullname" . }}
  labels:
    {{- include "meridian-frontend.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meridian-frontend.selectorLabels" . | nindent 4 }}
